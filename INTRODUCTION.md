# CoMind 简介

CoMind 是一款开源 AI 桌面助手，将截图识别、网页采集、资料整理、会议转写、模型问答和模拟面试集中在一个工作空间中，适合日常办公、学习与面试练习。

完整图文说明见 [CoMind 使用与原理指南](docs/CoMind-使用与原理指南.md)，包含启动配置、操作步骤、实现原理、故障排查和 10 张真实界面截图（使用虚构演示数据）。

## 主要功能

- **截图问答**：快捷键直接截取鼠标所在整屏，自动将图片发送给当前启用的视觉模型，识别内容并生成回答，无需圈选。
- **独立悬浮窗**：置顶展示回答或提词稿，支持透明度、字号和鼠标穿透设置，也可放在另一块屏幕上。
- **网页采集**：通过 Chrome 扩展将网页文字发送到桌面端。
- **灵活接入模型**：支持 OpenAI 兼容接口和 Anthropic Messages，可配置豆包、DeepSeek、GLM 等服务。
- **资料与复盘**：管理简历、职位描述、固定问答和提词稿，保存会话、生成复盘并导出 Markdown。
- **简历上传**：支持 PDF、Word 和图片解析，结果可编辑核对后保存。
- **会议语音识别**：macOS 采集系统播放的声音，使用百炼实时转写，可自动识别完整问题并生成回答。
- **AI 模拟面试**：根据简历与 JD，按校招／P6／P7／P8 训练档位逐题提问、追问，支持文字与麦克风作答，结束后生成颜色评分、STAR 参考答案和训练建议。
- **手机查看**：同一局域网扫码，只读查看普通会话问答；不自动同步模拟面试资料。
- **自定义快捷键**：支持全局快捷键配置，截图快捷键可直接按键录入。

## 本地启动

建议使用已验证的 Node.js 24：

```bash
git clone https://github.com/azhsmesos/CoMind.git
cd CoMind
npm ci
npm run dev
```

启动后进入「应用设置 → AI 模型连接」，填写自己的 API Key、接口地址和模型 ID。截图识别需要支持图片输入的模型；macOS 首次截图需授予屏幕录制权限。

## 技术与数据

基于 Electron、React、TypeScript 和 Vite，提供 macOS 与 Windows 打包配置。资料和会话保存在本机，生成回答时相关内容会发送给用户配置的模型服务。悬浮窗内容保护不能保证在所有录屏或屏幕共享工具中隐藏。

语音识别另需百炼连接，系统朗读使用本地中文音色；不保存原始录音。模拟面试档位是 CoMind 训练标准，不代表阿里官方考核标准，参考答案的缺失指标保留为「待补充」。

项目基于 [Open Interview Assistant](https://github.com/harry-the-nerd/open-interview-assistant) 扩展，遵循 Apache-2.0，保留原项目版权与 NOTICE。开发与构建命令见 [README](README.md)。
